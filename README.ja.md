# i18next-turbo ⚡️

**Rust + SWC による超高速 i18next 翻訳キー抽出 — 10〜100 倍高速**

`i18next-turbo` は `i18next-parser` および `i18next-cli` の**超高速代替**です。Rust と SWC で構築され、数千ファイルを**ミリ秒**で処理します。

> **⚠️ 開発中**: 現在は Rust バイナリとして利用可能です。npm パッケージの配布は準備中です。

---

## 🚀 なぜ i18next-turbo？

### パフォーマンス比較

| ツール | エンジン | 処理時間（1k ファイル） | Watch モード |
|:---|:---|:---|:---|
| `i18next-parser` | Node.js (Babel/Regex) | **10–30秒** | 遅い / 高負荷 |
| `i18next-cli` | Node.js (SWC) | **2–5秒** | 中程度 |
| **`i18next-turbo`** | **Rust + SWC** | **< 100ms** ⚡️ | **即時 / 低負荷** |

### 主な特徴

- ⚡️ **超高速**: 大規模プロジェクトでも即時処理
- 🎯 **高精度**: SWC による完全 AST 解析で誤検出ゼロ
- 🔄 **リアルタイム更新**: Watch モードで保存と同時に JSON を更新
- 🛡️ **既存翻訳を保持**: 新キーのみ追加、既存翻訳は触らない
- 📦 **軽量**: 低メモリ、バックグラウンド実行に適している
- 🔧 **i18next 互換**: 名前空間、複数形、コンテキストなどをサポート

---

## ✨ 実装済み機能

- `t('...')` / `i18n.t('...')` 抽出
- `<Trans>` の `i18nKey` / `defaults` 対応
- 複数形・コンテキスト（ICU ベース、言語別カテゴリ）
- マジックコメント、ネストキー、キー自動ソート
- TypeScript 型生成、デッドキー検出

---

## 📦 インストール

### Cargo でインストール（推奨）

```bash
cargo install i18next-turbo
```

### ソースからビルド

```bash
git clone https://github.com/your-username/i18next-turbo.git
cd i18next-turbo
cargo build --release
```

---

## 🛠️ 使い方

1. プロジェクトルートに `i18next-turbo.json` を作成（`input` / `output` / `locales` 等を設定）
2. **抽出**: `i18next-turbo extract`
3. **Watch**: `i18next-turbo watch`
4. **型生成**: `i18next-turbo typegen` または `i18next-turbo extract --generate-types`
5. **状態確認**: `i18next-turbo status`、**チェック**: `i18next-turbo check`

詳細は英語版 [README.md](./README.md) を参照してください。
